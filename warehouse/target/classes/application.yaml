server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  application:
    name: warehouse
  datasource:
    url: "jdbc:postgresql://localhost:5432/warehouse_identity"
    hikari:
      username: postgres
      password: linh191004
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true